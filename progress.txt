# Ralph Progress Log
Started: Tue 24 Mar 2026 20:49:25 PDT

## Codebase Patterns
- Project uses two Next.js route groups: `(frontend)` for public site, `(payload)` for admin/API
- PayloadCMS admin importMap is auto-generated via `pnpm generate:importmap` — do NOT edit `src/app/(payload)/admin/importMap.js` manually
- SQLite database stored at `data/payload.db` (gitignored)
- Tailwind CSS 4 uses `@import 'tailwindcss'` in CSS (no tailwind.config.js needed), configured via `postcss.config.mjs` with `@tailwindcss/postcss`
- PayloadCMS collections go in `src/collections/`, globals go in `src/globals/`
- Use `@/*` path alias for imports from `src/`
- Use `@payload-config` path alias for payload config import
- Seed script uses `tsx` to run TypeScript directly with path alias support (`pnpm seed`)
- `data/` directory must exist before SQLite can connect — seed or dev server creates the DB file
- PayloadCMS richText (Lexical) format: `{ root: { type: 'root', children: [{ type: 'paragraph', children: [{ type: 'text', text, version: 1 }], direction: 'ltr', format: '', indent: 0, version: 1 }], direction: 'ltr', format: '', indent: 0, version: 1 } }`
- Media uploads via Local API need `file: { data: Buffer, name, mimetype, size }` — use `fs.readFileSync`
- Make seed functions idempotent by checking for existing records before creating (query by unique field like `alt` or `title`)

---

## 2026-03-24 - US-001
- Scaffolded Next.js 15 + PayloadCMS 3.x project with SQLite
- Files changed: package.json, tsconfig.json, next.config.ts, postcss.config.mjs, .gitignore, .env, src/payload.config.ts, src/collections/Users.ts, src/collections/Media.ts, src/app/(frontend)/{layout,page,styles}.tsx/.css, src/app/(payload)/ admin+api routes
- **Learnings for future iterations:**
  - PayloadCMS 3.80.0 requires Next.js `>=15.2.9 <15.3.0 || >=15.3.9 <15.4.0 || >=15.4.11 <15.5.0 || >=16.2.0` — pinned to 15.4.11
  - `create-payload-app` fails in non-TTY environments — manual setup works fine
  - SQLite adapter uses `@libsql/client` Config type: `{ client: { url: 'file:./data/payload.db' } }`
  - Each route group `(frontend)` and `(payload)` has its own `layout.tsx` with `<html>`/`<body>` — no root layout needed
  - Must run `pnpm generate:importmap` after changing payload config before build
---

## 2026-03-24 - US-002
- Created siteSettings global with all required fields: clubName, address, phone, email, aboutText (richText), volunteerText (richText), heroImage, logo, lovemyclubBaseUrl, appStoreUrl, playStoreUrl
- Media collection already existed from US-001 with alt text field — no changes needed
- Registered SiteSettings global in payload.config.ts
- Files changed: src/globals/SiteSettings.ts (new), src/payload.config.ts
- **Learnings for future iterations:**
  - PayloadCMS globals use `GlobalConfig` type from `payload`, registered via `globals` array in buildConfig
  - Global slug uses kebab-case (`site-settings`) — this becomes the API endpoint and admin URL
  - Always run `pnpm generate:importmap` after modifying payload config
- PayloadCMS collections use `admin.useAsTitle` to set which field displays as the title in the admin list view
- Use `defaultSort` on collections to set default query ordering (e.g., `defaultSort: 'sortOrder'`)
- `select` field options use `{ label, value }` format
---

## 2026-03-24 - US-003
- Created coaches collection with: name, role, email, phone, photo, rates (richText), qualifications (array of text), bio (richText), sortOrder (default 0)
- Created programs collection with: name, slug (unique), ageRange, description (richText), schedule (array: day+time), registrationUrl, category (select: kids|girls|wheelchair|adult), season, year
- Registered both in payload.config.ts
- Files changed: src/collections/Coaches.ts (new), src/collections/Programs.ts (new), src/payload.config.ts
- **Learnings for future iterations:**
  - Collections with `defaultSort` automatically order queries — no need to manually sort in API calls
  - Array fields in PayloadCMS need a `fields` array defining sub-fields (e.g., qualifications array has a `qualification` text sub-field)
  - No import map regeneration needed when only adding new collections (no admin UI component changes)
---

## 2026-03-24 - US-004
- Created tournaments collection with: name (required), startDate, endDate, description (richText), registrationUrl, registrationDeadline, sanctionedBy, year. Default sort by startDate.
- Created sponsors collection with: name (required), tier (select: platinum|gold|silver|supporter), logo (upload), website, sortOrder (default 0). Default sort by sortOrder.
- Created announcements collection with: title (required), body (richText), active (checkbox, default true), priority (default 0), startDate, endDate. Default sort by -priority (descending).
- Registered all three in payload.config.ts
- Files changed: src/collections/Tournaments.ts (new), src/collections/Sponsors.ts (new), src/collections/Announcements.ts (new), src/payload.config.ts
- **Learnings for future iterations:**
  - Prefix sort field with `-` for descending order (e.g., `-priority` for highest priority first)
  - Confirmed: no import map regeneration needed when only adding collections
---

## 2026-03-24 - US-005
- Created seed script (`src/seed.ts`) with idempotent seeding for media, site settings, and announcements
- Uploaded logo.jpg, apple-app-store-badge.png, google-play-badge.png to media collection
- Populated siteSettings global with all club info, aboutText, volunteerText from site-report.md
- Created two announcements: "Memberships for 2026 are Sold Out" and "Spring programs are live for registration"
- Added `pnpm seed` command using `tsx` to run TypeScript seed script
- Added `tsx` as dev dependency
- Files changed: src/seed.ts (new), package.json
- **Learnings for future iterations:**
  - `payload run` swallows stdout — use `tsx` directly for seed scripts that need console output
  - `data/` directory must exist before SQLite adapter can connect
  - Lexical richText format requires specific structure with `version: 1` on text nodes
  - Export `seed()` function for composition — future seed files can import and extend
---
